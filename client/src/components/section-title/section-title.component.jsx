import React from "react";

import "./section-title.style.scss";

const SectionTitle = ({ titleText }) => {
  return (
    <div className="section-title">
      <h2>สินค้าเด่นวันนี้</h2>
    </div>
  );
};

export default SectionTitle;
